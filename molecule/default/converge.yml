# terraform managed
---
- name: Converge
  hosts: all
  gather_facts: true

  pre_tasks:
    - name: Check if test vars are present
      ansible.builtin.include_vars:
        file: test.yaml

  tasks:
    - name: "Include pimvh.ssh_keygen"
      ansible.builtin.include_role:
        name: "ssh_keygen"
